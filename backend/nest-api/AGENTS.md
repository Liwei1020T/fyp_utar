# AGENTS.md - nest-api

> Auto-generated by `codex-init-agents` on 2026-04-03.  
> Keep this file short, executable, and updated with project changes.

## Scope

- This file applies to this directory and all children.
- Deeper `AGENTS.md` overrides this file in subdirectories.

## Project Context

- Mission: [1-2 lines]
- Primary users: [who this serves]
- Non-goals: [what not to optimize for]

## Validation Commands

- Build: `npm run build` (or project equivalent)
- Lint: `npm run lint`
- Typecheck: `npm run type-check` (or `npm run tsc`)
- Tests: `npm test`
- Fast loop for touched areas first, then run full suite before completion.

## Architecture Map

- Entry points: [main app/server/cli files]
- Core modules: [key directories and responsibilities]
- Critical paths: [hot or high-risk flows]
- State/data boundaries: [DB/cache/external APIs]

## Change Rules

1. Prefer minimal diffs that solve requested scope.
2. Reuse existing patterns before introducing new abstractions.
3. Update docs/tests when behavior changes.
4. Never commit secrets or private credentials.

## Definition of Done

1. Requested behavior is implemented.
2. Relevant checks pass (or explicitly marked `unverified` with reason).
3. No known regressions introduced.
4. Risks, assumptions, and follow-ups are documented in final update.

## High-Risk Changes (Ask Before Proceeding)

- Destructive operations (`rm -rf`, hard reset, history rewrite, force push)
- Irreversible data/schema migrations
- Production auth/security/infra changes
- Large dependency/tooling upgrades

## Quick Start (Customize Me)

- Setup: [install steps]
- Run locally: [dev command]
- Test locally: [test command]
- Release/deploy: [if applicable]
